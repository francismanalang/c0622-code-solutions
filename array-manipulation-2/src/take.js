/* exported take */
// define the function
// use the slice method to return an array that starts at 0 and ends at count
// return the new slice variable from the function

function take(array, count) {
  return array.slice(0, count);
}
