/* exported take */
// define the function
// use the slice method to return the array at count - 1
// return the new slice variable from the function

function take(array, count) {
  return array.slice(count);
}
