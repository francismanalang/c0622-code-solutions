/* exported drop */
// define the function
// use the slice method and set the argument equal to count
// return the result from the function

function drop(array, count) {
  return array.slice(count);
}
