/* exported getFirstChar */
// define the function
// take the parameter string at index 0 and assign it to the variable firstChar
// return firstChar from the function

function getFirstChar(string) {
  var firstChar = string[0];
  return firstChar;
}
